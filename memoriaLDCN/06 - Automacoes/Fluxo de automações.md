# Fluxo de automações

Pedido do usuário → detecção do tipo → identificação do gatilho → identificação das ações → solicitação de credenciais → geração do fluxo → teste com dados simulados → ativação → monitoramento.

Automação possui ciclo de vida próprio e não deve ser tratada apenas como geração de código.
