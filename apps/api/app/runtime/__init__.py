"""LDCN Multi-Agent Runtime -- catalog and shared contracts over the agent/
orchestrator infrastructure that already exists across apps/api/app/engines.

Phase 0 (see the architecture proposal): this package is additive and
read-only. It does not change the behavior of any engine it catalogs."""
