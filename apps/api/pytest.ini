[pytest]
markers =
    slow: long-running tests excluded from the normal development cycle
    enterprise: Enterprise-scale proof tests (>100k files)
    integration: tests that require explicitly provisioned external services
    contract: cross-boundary API or infrastructure contract tests
    security: adversarial security and isolation tests
addopts = -m "not slow and not enterprise and not integration"