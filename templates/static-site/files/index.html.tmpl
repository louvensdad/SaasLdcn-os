<!doctype html>
<html lang="{{project.locale}}">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{project.name}}</title>
    <link rel="stylesheet" href="./styles.css" />
  </head>
  <body>
    <main>
      <p>{{template.name}}</p>
      <h1>{{project.name}}</h1>
      <section>
        <h2>Static foundation</h2>
        <p>{{project.summary}}</p>
      </section>
    </main>
  </body>
</html>
