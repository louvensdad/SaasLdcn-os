# Engineering Presence Layer — Phase 2

Implemented derived operational presence from `GenerationJob` and `ActivityEvent`, with no new state table. Added priority resolution, evidence decisions, workspace authorization, stale-safe frontend surfaces, and sandbox result events.

The new `SilentIntelligencePanel` is read-only and institutional: no chat, avatar, voice, prompt field, or LLM call.