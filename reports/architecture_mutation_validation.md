# Architecture Mutation Validation

## Mutation behavior
- `microservices` mutates dependency requirements so observability becomes required and deployment burden rises.
- `ai_chat` / `rag` mutations surface vector infrastructure, cache, queue, and tracking-related recommendations.
- `payments` mutation increases security-oriented recommendations.

## Validation summary
- Mutations are reflected in the dependency graph snapshot.
- Mutated recommendations feed the wizard and blueprint surfaces without generating code.

