# Premium Typography Validation

The shared system defines Display (48px), Page (32px), Section (24px), Card (18px), Body (15px), Caption (13px), and Label (12px) roles.

`SectionHeader`, Wizard step headers, Architecture Studio summaries, LDCN rail, sidebar, and engineering surfaces use these roles. Excessive uppercase and letter spacing were removed from the shared engineering surfaces and primary shell.
