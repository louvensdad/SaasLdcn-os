# Architecture Lab

## Implemented

- Builds top-level module nodes from the real project file tree.
- Builds import edges from static import/from statements.
- Displays nodes and coupling evidence in the Architecture module.

## Limits

The current graph is static and heuristic. Runtime topology, queues, gateways and microservice boundaries require deeper framework-specific analyzers.
